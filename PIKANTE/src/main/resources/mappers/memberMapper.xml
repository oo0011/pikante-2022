<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pikante.mappers.member">

	<!-- 게시물 목록 -->
	<select id="list" resultType="com.pikante.domain.MemberVO">
		select
		number, name, position, birthday
		from member
	</select>
</mapper>